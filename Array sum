#include <iostream>
using namespace std;

int main() {
    int n;
    cin >> n;
    int b[1000];
    for(int i=0;i<n;i++){
        cin >>b[i];
    }
   for (int i=0;i<n;i++)
   cout << b[i] << endl;
   
   int sum;
   sum = 0;
   for ( int i=0;i<n;i++)
   sum=sum +b[i] ;
   cout << sum <<endl;
}
